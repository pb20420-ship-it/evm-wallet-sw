name = "evm-wallet-sw"
compatibility_date = "2024-09-16"
compatibility_flags = ["nodejs_compat"]

# Serve the Vite-built frontend as static assets (SPA)
[assets]
directory = "./dist-ui"
not_found_handling = "single-page-application"
